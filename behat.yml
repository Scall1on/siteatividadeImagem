default:
    suites:
        default:
            contexts:
                - FeatureContext
    extensions:
        Behat\MinkExtension:
            base_url: 'http://localhost:8001'
            sessions:
                default:
                    browserkit_http: ~
